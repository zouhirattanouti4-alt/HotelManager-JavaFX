# =====================================================================
# Configuration de la connexion MySQL — HotelManager
# ---------------------------------------------------------------------
# Modifiez ces valeurs selon votre installation locale, puis relancez
# l'application. Si ce fichier est absent, des valeurs par défaut
# équivalentes sont utilisées (voir Database.java).
# =====================================================================

db.url=jdbc:mysql://localhost:3306/hotel_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
db.user=root
db.password=
